# devenv — generated and machine-specific
state/registry.tsv
state/shellents.tsv
state/logs/
shell/*.zsh

# keep these tracked
!state/deferred.tsv
!.gitkeep

.DS_Store

# Secrets and private audit material — keep this protection in-repo so it
# travels to collaborators, CI, and fresh clones.
*.key
.env
.env.*
*.pem
*.p12
AUDIT-keys.md
